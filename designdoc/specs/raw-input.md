# 原始业务输入（raw-input）

> **用途**：② 业务需求确认的必读输入来源登记。  
> **维护**：① 流程初始化时创建；规格变更时由人更新。

## 主文档

| 项 | 值 |
|----|-----|
| **路径** | [`designdoc/整体设计.md`](../整体设计.md) |
| **版本** | 1.0.0-replay |
| **分支** | `replay/4p12s-from-design` |
| **状态** | 输入已确认（供重跑） |

## 说明

- ②～⑥ **须从该文件再拆**规格（requirements-register / prd / user-stories / design / verification-plan）。
- **禁止**整包复制 `develop` 或其他分支的旧 specs 冒充重跑产出。
- ⑧ 起才按 `整体设计.md` §2 创建 `apps/`、`packages/` 脚手架。

## ② 读取要点

从 `整体设计.md` 提取：

1. 产品定位与范围内/外（§1）
2. 固定技术栈与模块边界（§2）
3. 角色与验收场景 AC（§3）
4. 业务规则 BR（§4）
5. 信息架构与数据模型摘要（§5～§6）
6. 非功能要求 NFR（§7）
