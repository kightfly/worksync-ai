# TDD 强制规则

## 核心原则

1. **先写测试，后写实现**
   - 禁止在无测试的情况下实现功能
   - 测试失败后才能开始实现

2. **测试覆盖所有业务逻辑**
   - 领域层：100% 覆盖
   - 应用层：90%+ 覆盖
   - API 层：85%+ 覆盖
   - 页面层：80%+ 覆盖

3. **测试必须独立**
   - 测试之间不可互相依赖
   - 每个测试可独立运行

## 测试命名规范

```typescript
// ✅ 好的命名
describe('User', () => {
  it('邮箱格式无效时抛出错误', () => {});
  it('密码长度不足时抛出错误', () => {});
  it('成功创建用户', () => {});
});

// ❌ 坏的命名
describe('User', () => {
  it('test1', () => {});
  it('should work', () => {});
});
```

## 测试结构 (AAA 模式)

```typescript
it('计算订单总价', () => {
  // Arrange - 准备数据
  const order = new Order();
  order.addItem({ price: 100, quantity: 2 });
  
  // Act - 执行操作
  const total = order.calculateTotal();
  
  // Assert - 断言结果
  expect(total).toBe(200);
});
```

## 边界条件测试

必须测试边界条件：

```typescript
// 密码长度校验
it('密码长度为 5 时报错', () => {});
it('密码长度为 6 时通过', () => {});
it('密码长度为 100 时通过', () => {});

// 日期范围
it('开始日期晚于结束日期时报错', () => {});
it('日期范围为空时返回空数组', () => {});
```

## 错误路径测试

必须测试错误情况：

```typescript
// 必填项缺失
it('标题为空时创建任务失败', () => {});

// 格式错误
it('邮箱格式错误时登录失败', () => {});

// 权限不足
it('未授权用户访问 API 返回 401', () => {});
```

## 禁止事项

- ❌ 禁止修改测试使实现通过（测试优先）
- ❌ 禁止跳过失败测试
- ❌ 禁止测试依赖执行顺序
- ❌ 禁止测试中包含异步操作而不等待

## 验收标准

每个任务完成前必须：
1. 运行测试并通过
2. 检查测试覆盖率
3. 确认边界条件已覆盖
4. 确认错误路径已测试
