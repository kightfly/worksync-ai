# 安全规范

## 核心原则

1. **默认拒绝不安全默认值** — 认证、授权、密钥不得硬编码进仓库
2. **最小权限** — API 与 DB 账户仅授予所需权限
3. **输入不可信** — 所有外部输入经校验（zod / 领域规则）后再使用
4. **密钥不入库** — `.env`、凭证、私钥不得提交；使用 `.env.example` 说明变量名

## 认证与会话

- 密码：存储必须哈希（禁止明文）；长度与复杂度按需求规格
- 会话 / JWT：密钥来自环境变量；设置合理过期；登出须失效客户端态
- 错误提示：登录失败使用日文通用文案，避免泄露「用户是否存在」等细节（按产品规格取舍）

## API 与数据

- 所有需登录接口必须鉴权；禁止仅靠前端隐藏按钮
- 禁止 SQL 字符串拼接用户输入；使用 Drizzle 参数化查询
- 禁止把内部堆栈直接返回给客户端（生产）
- 文件/导出类功能须校验权限与大小限制（若后续实现）

## 前端

- 不在前端存储长期密钥；Token 存放策略须可审查
- 渲染用户内容时注意 XSS（React 默认转义；避免 `dangerouslySetInnerHTML` 除非已消毒）
- 表单必填与格式校验不得省略

## Agent / 交付门禁

- 不得在 commit 中包含 `.env`、密钥、生产连接串
- 安全相关改动须有测试或检查清单回写到 TASK 证据
- 技术设计（⑤）须写明鉴权边界；验证计划（⑥）须覆盖未授权访问场景

## 检查清单（PR / 宣称完成前）

- [ ] 无密钥入库
- [ ] 敏感接口有鉴权
- [ ] 输入有校验
- [ ] 密码非明文存储
- [ ] 错误信息不泄露内部细节
