# 4p12s 门禁规则

## 总则

1. **主链路是四阶十二步**（见 `AGENTS.md`）。跳步实现功能视为违规，除非人明确授权 hotfix，并事后回写状态。
2. **文件化**：进度只认 `designdoc/delivery/delivery-state.md`，不认聊天里的「已经做完了」。
3. **无新鲜验证证据，不得宣称完成**（Superpower：verification-before-completion）。

## 逐步门禁

| 步 | 未满足则 |
|----|----------|
| ① | 无状态表 / 无测试护栏约定 → 不进入需求实现 |
| ② | 范围、规则、验收场景不可追溯 → 不进 PRD |
| ③ | 规格未经确认 → 不进用户故事 |
| ④ | 缺异常/边界或不可测 → 不进技术设计 |
| ⑤ | 模块边界或接口契约不清 → 不拆任务 |
| ⑥ | 关键风险未覆盖或禁 Mock 范围不清 → 不进 TDD 实现 |
| ⑦ | 任务不能独立验证或缺验收 → 不进编码 |
| ⑧ | 无失败测试不写生产代码；**连续 5 次红灯 → 停止并升级给人** |
| ⑨ | 仅 Mock / 仅单测 → 假完成，不进入 E2E 宣称通过 |
| ⑩ | E2E 未禁 Mock 或主流程失败 → 不 Git 宣称交付完成 |
| ⑪ | 需求/设计/开发/测试未对齐 → 不推送「完成」 |
| ⑫ | 无部署记录或环境不可访问 → 交付未完成 |

## Agent 行为

- 开始任务前：读 `delivery-state.md` 与 `harness-alignment-status.md`
- 若当前步骤 Skills 文件仍为「待建」：按 `AGENTS.md` 门禁摘要执行，并在回写中注明「Skill 未落地」
- 每步产出路径变更后：更新 `delivery-state.md` 对应行
- Harness 方法论工作（补 Skills/CI 等）：更新 `harness-alignment-status.md`

## 与 TDD / 安全 / 时区

- ⑧ 必须遵守 `tdd.mdc`
- 涉及鉴权与密钥遵守 `security.mdc`
- 涉及日期时间遵守 `timezone.mdc`

## 假完成定义（禁止）

- 只跑过单测就说「功能完成」
- 集成/E2E 全 Mock 却说「真实链路验证通过」
- 未更新 delivery-state 却说「十二步已推进」
