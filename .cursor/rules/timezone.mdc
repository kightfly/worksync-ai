# 时区处理规则

## 固定策略

| 层 | 规则 |
|----|------|
| 数据库存储 | **UTC**（`TIMESTAMP WITH TIME ZONE`） |
| 前端展示 | **Asia/Tokyo**（UTC+9） |
| API 交换 | ISO-8601；带时区偏移或 Z；文档中写明约定 |
| 业务「工作日」 | 按 Asia/Tokyo 日历日解释（打卡、任务截止日期） |

## 禁止事项

- 禁止在 DB 存「无时区的本地时间」并假设服务器在东京
- 禁止前端用 `new Date(string)` 解析含糊日期串而不声明时区
- 禁止混用多个展示时区（本项目固定 Tokyo）

## 实现要点

### 写入

- 用户输入的东京本地时间 → 转换为 UTC 再持久化
- 领域层优先使用明确的时间类型 / 工具函数，集中转换逻辑

### 读出

- API 返回 UTC（ISO）或明确偏移
- UI 使用 `Intl.DateTimeFormat` 或项目日期工具，`timeZone: 'Asia/Tokyo'`

### 测试

- 单测覆盖：跨日边界（例：东京 0 点前后）、夏令时无关（日本无 DST）但仍测偏移换算
- 测试数据构造时写明「这是东京墙钟还是 UTC」

## 与十二步的关系

- ⑤ 技术设计须重申本策略
- ⑥ 验证计划须含时区相关用例（打卡、截止日期展示）
- ⑧ 实现不得绕过共享日期工具另写一套

## 检查清单

- [ ] Schema 时间列带 time zone
- [ ] 展示为 Asia/Tokyo
- [ ] 输入转换路径有测试
- [ ] 文档 / API 契约与实现一致
